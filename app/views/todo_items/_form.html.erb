<%= form_for([@todo_list, @todo_list.todo_items.build]) do |f| %>
  <%= f.text_field :content,{:class => "form-control"} %>
  <%= f.text_field :priority_number, {:class => "form-control-sm"} %>
  <%= f.submit({:class => "btn btn-primary"})%>
<% end %>
